version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - echo "STARTING"

  build:
    commands:
      - echo "Zipping project into build_output.zip..."
      - zip -r build_output.zip . 

artifacts:
  files:
    - build_output.zip
